# GitHub Pages 웹 버전

이 폴더는 서버 없이 브라우저에서 실행되는 정적 버전입니다.

- 지원: PDF 텍스트 추출, 스캔 PDF OCR, 이미지 OCR, TXT/MD 정리
- 미지원: HWP/HWPX/PPTX 고품질 변환
- 외부 라이브러리: jsDelivr CDN의 `pdfjs-dist`, `tesseract.js`

GitHub Pages 설정에서 배포 소스를 `main` 브랜치의 `/docs` 폴더로 지정하면 됩니다.
